---
phase: 02-source-grounded-retrieval-and-safety-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb/nutrition/nutrition-basics.md
  - kb/programming/hypertrophy-principles.md
  - kb/safety/safety-boundaries.md
  - kb/vitruvian/membership-features.md
  - kb/vitruvian/strength-assessment-weight-cap.md
  - kb/vitruvian/training-modes.md
  - src/prompts.ts
  - src/safety.ts
files_forbidden:
  - src/retrieval.ts
  - src/index.ts
  - tests/phase1-contract.test.mjs
  - tests/phase2-retrieval-safety.test.mjs
sequential_files: []
expected_artifacts:
  - path: "src/prompts.ts"
    provides: "Isolated system prompt module exporting buildSystemPrompt function"
    required: true
  - path: "src/safety.ts"
    provides: "Physiological safety gate pre/post-filters for user coaching requests"
    required: true
autonomous: false
agents:
  - engineering-ai-engineer
  - testing-qa-verification-specialist
requirements:
  - R7
user_setup: []
verification_commands:
  - "npm run typecheck"
  - "node -e \"import('./src/prompts.ts').then(m => { if (typeof m.buildSystemPrompt !== 'function') throw new Error('Missing buildSystemPrompt export'); })\""
  - "node -e \"import('./src/safety.ts').then(m => { if (typeof m.checkSafetyRedFlags !== 'function') throw new Error('Missing checkSafetyRedFlags export'); })\""
  - "grep -q 'disclaimer:' kb/nutrition/nutrition-basics.md"
  - "grep -q 'disclaimer:' kb/safety/safety-boundaries.md"

must_haves:
  truths:
    - "All 6 KB markdown files under kb/ contain standardized YAML frontmatter including title, source, version, and disclaimer."
    - "All 6 KB markdown files contain a footnoted medical disclaimer: 'Disclaimer: This document is for general coaching and educational purposes only. It is not medical advice.'"
    - "src/prompts.ts exports buildSystemPrompt(profileString, contextString) that returns the fully formed coaching system instruction string."
    - "src/safety.ts exports checkSafetyRedFlags(message) returning an object { safe: boolean; reason?: string; responseOverride?: string } indicating if physiological/safety bounds were crossed."
  artifacts:
    - path: "src/prompts.ts"
      provides: "System prompt instructions isolated"
      min_lines: 40
      contains: "highly skilled, expert AI athletic"
    - path: "src/safety.ts"
      provides: "Pre-filtering safety routines"
      min_lines: 30
      contains: "checkSafetyRedFlags"
  key_links: []
---

<objective>
Isolate and harden the Coach's prompts and establish robust pre-filtering for sensitive health or medical inputs (pain, injuries, supplement abuse, extreme dieting), while adding disclaimers and source metadata directly into the seed KB files.

Purpose: Separate concerns by removing hardcoded system instructions from src/index.ts into src/prompts.ts, and create a centralized safety barrier in src/safety.ts to meet R7.
Output: src/prompts.ts, src/safety.ts, and 6 updated files in kb/.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-source-grounded-retrieval-and-safety-behavior/02-CONTEXT.md
Relevant source files:
- src/index.ts
- kb/**/*.md
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Lead AI Engineer (implementation) and QA Verification Specialist (validation)
Task: Standardize KB file metadata and implement isolated system prompts and safety gates (R7).
Scope:
- Read targets: src/index.ts, all 6 files under kb/
- Write targets: kb/**/*.md, src/prompts.ts, src/safety.ts
- Forbidden targets: src/retrieval.ts, src/index.ts, tests/*
Allowed tools/actions:
- File reads, file writes, typechecks, regex search, and Node.js imports sanity checks.
Forbidden actions:
- Do not modify src/index.ts or create test suites yet (that is Plan 02-03).
- Do not add external NPM dependencies.
Implementation sequence:
1. Read the contents of all 6 KB files under kb/.
2. Update all 6 KB files: prepend standardized YAML frontmatter blocks and append standard footnoted disclaimers.
3. Extract `buildSystemPrompt` function logic and constants from src/index.ts lines 362-420. Write it into a new file src/prompts.ts.
4. Create src/safety.ts. Implement `checkSafetyRedFlags(message: string)` that analyzes user queries for terms like 'pain', 'injury', 'chest pain', 'starve', 'clenbuterol', etc. If detected, return { safe: false, reason: 'red_flag', responseOverride: '...' } with a firm medical advice disclaimer and steering instructions.
5. Verify compile correctness with `npm run typecheck` and verify module exports via inline Node.js commands.
Required interfaces/content structure:
- src/prompts.ts must export:
  `export function buildSystemPrompt(profileString: string, contextString: string): string`
- src/safety.ts must export:
  `export function checkSafetyRedFlags(message: string): { safe: boolean; reason?: string; responseOverride?: string }`
Edge/error cases:
- Empty query or case variations (e.g., 'PAIN', 'I am starving') must be normalized and successfully flagged.
- Safe words that contain red-flag roots (e.g. 'paint') must not trigger false positives.
Verification criteria:
- All updated files compile successfully.
- Tests verify frontmatter is present in KB files.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Any file in kb/ is unreadable or fails write permissions.
- Part of buildSystemPrompt requires variables that are not yet isolated.
- The typescript compiler (`npm run typecheck`) fails on your changes.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and any RESULT/SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Update all KB markdown files with metadata and disclaimers</name>
  <files>
    - kb/nutrition/nutrition-basics.md
    - kb/programming/hypertrophy-principles.md
    - kb/safety/safety-boundaries.md
    - kb/vitruvian/membership-features.md
    - kb/vitruvian/strength-assessment-weight-cap.md
    - kb/vitruvian/training-modes.md
  </files>
  <action>
Read each of the 6 markdown files in kb/.
For each file:
1. Prepend standard YAML frontmatter:
---
title: "{Descriptive Title matching filename}"
source: "Vitruvian Official Knowledge Base"
version: "1.0"
last_updated: "2026-05-27"
disclaimer: "This document is for general coaching and educational purposes only. It is not medical advice, diagnosis, or treatment."
---
2. Append a standard footnote disclaimer to the end of the file:
---
*Disclaimer: This document is for general coaching and educational purposes only. It is not medical advice, diagnosis, or treatment.*

> verification: grep -q 'disclaimer:' kb/nutrition/nutrition-basics.md
> verification: grep -q 'Disclaimer: This document' kb/nutrition/nutrition-basics.md
  </action>
  <verify>
grep -q 'disclaimer:' kb/nutrition/nutrition-basics.md
grep -q 'Disclaimer: This document' kb/nutrition/nutrition-basics.md
grep -q 'disclaimer:' kb/safety/safety-boundaries.md
  </verify>
  <done>All 6 KB files have standardized frontmatter metadata and standard medical disclaimers appended.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/prompts.ts to isolate system prompt generation</name>
  <files>
    - src/prompts.ts
  </files>
  <action>
Create src/prompts.ts.
Copy the system prompt structure from src/index.ts lines 362-420.
Add strict instruction directives inside the system prompt:
1. **Source Grounding (R6):** State clearly that if the user asks about Vitruvian-specific capabilities (e.g. membership features, strength limits, motor modes, computer spotting, connection heartbeat enums) and the retrieved KB context is absent, incomplete, or does not support the facts, the Coach MUST refuse to speculate and instead state: "I do not have the official Vitruvian records on this. Please consult the official Vitruvian documentation."
2. **Medical/Physiological Safety (R7):** Instruct the LLM that it must NEVER diagnose, treat, or suggest unsafe actions. If the user indicates pain, chest discomfort, joint pain, or starvation targets, it must yield a conservative advisory and refuse training routines.

Export `buildSystemPrompt(profileString: string, contextString: string): string` from this module.

> verification: test -f src/prompts.ts
> verification: grep -q 'export function buildSystemPrompt' src/prompts.ts
> verification: grep -q 'Consult the official Vitruvian documentation' src/prompts.ts
  </action>
  <verify>
test -f src/prompts.ts
grep -q 'export function buildSystemPrompt' src/prompts.ts
grep -q 'Consult the official Vitruvian documentation' src/prompts.ts
  </verify>
  <done>src/prompts.ts successfully isolates system prompt formatting and incorporates strict grounding and medical safety directives.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/safety.ts to implement input safety gating</name>
  <files>
    - src/safety.ts
  </files>
  <action>
Create src/safety.ts.
Implement `checkSafetyRedFlags(message: string): { safe: boolean; reason?: string; responseOverride?: string }` to pre-screen user inputs.
Ensure it matches the following triggers (case-insensitive):
- **Pain / Chest Discomfort / Injury:** e.g., 'chest pain', 'joint pain', 'stabbing pain', 'injured my shoulder', 'hurts my chest'.
  Response Override: 'I am sorry to hear you are experiencing discomfort. As an AI athletic coach, I cannot provide medical advice, diagnosis, or treatment plans. If you are experiencing chest pain or acute joint discomfort, please stop exercising immediately and consult a healthcare professional. We cannot plan routines for active injuries without medical clearance.'
- **Extreme Dieting:** e.g., 'starve myself', 'dieting on 500 calories', 'eating only 800 kcal'.
  Response Override: 'Your safety is the highest priority. Restricting your intake to extreme calorie targets (such as under 1200 kcal/day for women or 1500 kcal/day for men) is highly counterproductive for performance, hypertrophy, and overall metabolic health. Please consult a registered dietitian or healthcare professional to establish a sustainable, safe nutrition plan.'
- **Unsafe Supplements:** e.g., 'clenbuterol', 'DNP', 'anator', or toxic compounds.
  Response Override: 'I cannot recommend or assist in scheduling routines that incorporate unsafe, unapproved, or high-risk chemical performance enhancers like DNP or clenbuterol. These substances carry extreme, life-threatening physiological risks. Let\'s focus on natural hypertrophy principles and progressive overload.'

Export this function. Also verify that running `npm run typecheck` passes.

> verification: test -f src/safety.ts
> verification: grep -q 'export function checkSafetyRedFlags' src/safety.ts
> verification: npm run typecheck
  </action>
  <verify>
test -f src/safety.ts
grep -q 'export function checkSafetyRedFlags' src/safety.ts
npm run typecheck
  </verify>
  <done>src/safety.ts contains active pre-filters for injuries, extreme dieting, and unsafe supplements, passing typechecks successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [x] All 6 KB files contain frontmatter title, source, version, and disclaimer.
- [x] src/prompts.ts is successfully created and exports buildSystemPrompt.
- [x] src/safety.ts is successfully created and exports checkSafetyRedFlags.
- [x] `npm run typecheck` passes with no compilation errors.
- [x] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Seed files under `kb/` contain standardized YAML headers.
- System prompts are cleanly modularized in `src/prompts.ts`.
- Safety filters for sensitive user requests are written and pass TypeScript typechecking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-grounded-retrieval-and-safety-behavior/02-01-SUMMARY.md` summarizing the updated KB files, prompt logic modularization, safety filters implemented, and verification results.
</output>
