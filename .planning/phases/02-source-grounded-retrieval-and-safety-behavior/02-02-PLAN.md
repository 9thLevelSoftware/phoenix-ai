---
phase: 02-source-grounded-retrieval-and-safety-behavior
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval.ts
files_forbidden:
  - kb/nutrition/nutrition-basics.md
  - kb/programming/hypertrophy-principles.md
  - kb/safety/safety-boundaries.md
  - kb/vitruvian/membership-features.md
  - kb/vitruvian/strength-assessment-weight-cap.md
  - kb/vitruvian/training-modes.md
  - src/prompts.ts
  - src/safety.ts
  - src/index.ts
  - tests/phase1-contract.test.mjs
  - tests/phase2-retrieval-safety.test.mjs
sequential_files: []
expected_artifacts:
  - path: "src/retrieval.ts"
    provides: "Dedicated retrieval parser, metadata compiler, and grounding validation module"
    required: true
autonomous: false
agents:
  - engineering-ai-engineer
  - testing-qa-verification-specialist
requirements:
  - R5
  - R6
user_setup: []
verification_commands:
  - "npm run typecheck"
  - "node -e \"import('./src/retrieval.ts').then(m => { if (typeof m.extractChunks !== 'function') throw new Error('Missing extractChunks export'); })\""
  - "node -e \"import('./src/retrieval.ts').then(m => { if (typeof m.sourceMetadata !== 'function') throw new Error('Missing sourceMetadata export'); })\""
  - "node -e \"import('./src/retrieval.ts').then(m => { if (typeof m.checkContextGrounding !== 'function') throw new Error('Missing checkContextGrounding export'); })\""

must_haves:
  truths:
    - "src/retrieval.ts exports extractChunks(searchResults: unknown) that returns parsed and normalized AISearchChunk objects."
    - "src/retrieval.ts exports sourceMetadata(chunks: AISearchChunk[]) assembling safe arrays of { id, source, score }."
    - "src/retrieval.ts exports formatContext(chunks: AISearchChunk[]) returning combined source-grounded articles as a single string."
    - "src/retrieval.ts exports checkContextGrounding(chunks: AISearchChunk[], reply: string) that performs post-generation validation on LLM output to detect unsupported product claims."
  artifacts:
    - path: "src/retrieval.ts"
      provides: "Retrieval parsing and grounding check"
      min_lines: 50
      contains: "checkContextGrounding"
  key_links: []
---

<objective>
Create a dedicated RAG helper module src/retrieval.ts containing search result normalization, metadata extraction, safe payload mapping, and automated post-generation fact-grounding checks to satisfy R5 and R6.

Purpose: Isolate retrieval mapping from controller endpoints, verify that search result formats (chunks, data, results) are properly normalized, and check that responses do not hallucinate facts absent from the context.
Output: src/retrieval.ts.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-source-grounded-retrieval-and-safety-behavior/02-CONTEXT.md
Relevant source files:
- src/index.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Lead AI Engineer (implementation) and QA Verification Specialist (validation)
Task: Implement the retrieval parsing, metadata assembly, and grounding validation module (R5, R6).
Scope:
- Read targets: src/index.ts
- Write targets: src/retrieval.ts
- Forbidden targets: kb/**/*.md, src/prompts.ts, src/safety.ts, src/index.ts, tests/*
Allowed tools/actions:
- File reads, creating src/retrieval.ts, typechecking, and export checks.
Forbidden actions:
- Do not edit files outside `src/retrieval.ts`.
- Do not import external NLP libraries.
Implementation sequence:
1. Copy RAG helper types and functions from src/index.ts (e.g. `normalizeChunk`, `extractChunks`, `sourceLabel`, `sourceMetadata`, `formatContext`). Write them into src/retrieval.ts.
2. In src/retrieval.ts, add robust support to ensure that if a retrieved chunk's raw content is empty, it returns a safe placeholder instead of crashing.
3. Implement `checkContextGrounding(chunks: AISearchChunk[], reply: string): { grounded: boolean; missingClaims?: string[] }`.
   This function performs a post-processing check on the LLM output. It checks if the reply makes specific assertions about Vitruvian capabilities that are not supported by the retrieved chunks.
   - For example: if the reply mentions training modes (like 'OLD_SCHOOL', 'TUT', 'Pump', etc.) or weight multipliers (display multiplier of 2, digital ceiling limits), it checks if those words exist in the text of the retrieved chunks. If they are in the reply but completely absent from retrieved chunks, flag it!
   - Under this condition, return `{ grounded: false, missingClaims: [...] }`.
4. Export all functions and verify typescript compilation.
Required interfaces/content structure:
- src/retrieval.ts must export:
  `export function extractChunks(searchResults: unknown): AISearchChunk[]`
  `export function sourceMetadata(chunks: AISearchChunk[]): JsonObject[]`
  `export function formatContext(chunks: AISearchChunk[]): string`
  `export function checkContextGrounding(chunks: AISearchChunk[], reply: string): { grounded: boolean; missingClaims?: string[] }`
Edge/error cases:
- Handles case where searchResults matches none of the expected array candidate fields (chunks, data, results) and falls back safely to empty array `[]`.
- Performs robust substring or token boundary checks for grounding keywords to avoid false alarms.
Verification criteria:
- Module compiles successfully.
- Node.js export checks pass.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Writing to src/retrieval.ts returns permission errors.
- Any chunk property mapping fails compile typechecks.
- Grounding logic contains complex external deps not supported by the Cloudflare Worker platform.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and any RESULT/SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create src/retrieval.ts to handle chunk processing and grounding validation</name>
  <files>
    - src/retrieval.ts
  </files>
  <action>
Create src/retrieval.ts.
Copy types (JsonPrimitive, JsonValue, JsonObject, AISearchChunk) and functions (`normalizeChunk`, `extractChunks`, `sourceLabel`, `sourceMetadata`, `formatContext`) from src/index.ts. Ensure they are fully typed.
Add the `checkContextGrounding(chunks: AISearchChunk[], reply: string): { grounded: boolean; missingClaims?: string[] }` function:
- Gather all lowercase text content from `chunks` into a single searchable context string.
- Define a list of Vitruvian-specific product keywords/claims to enforce grounding on:
  - Tiers: `ember`, `flame`, `inferno`
  - Modes: `old school`, `old_school`, `time under tension`, `tut_beast`, `pump`, `eccentric only`, `eccentric_only`, `echo`
  - Specifications: `display multiplier`, `weight_multiplier`, `spotter`, `slack limit`, `strength ceiling`, `0x50` (heartbeat Stop command), `withoutresponse`
- For each keyword/claim: if the keyword is mentioned in the LLM's `reply` (case-insensitive) but is NOT present in the retrieved chunks' combined text (case-insensitive), add it to `missingClaims`.
- If `missingClaims.length > 0`, return `{ grounded: false, missingClaims }`. Otherwise, return `{ grounded: true }`.

Export all required functions from the file.

> verification: test -f src/retrieval.ts
> verification: grep -q 'export function checkContextGrounding' src/retrieval.ts
> verification: npm run typecheck
  </action>
  <verify>
test -f src/retrieval.ts
grep -q 'export function checkContextGrounding' src/retrieval.ts
npm run typecheck
  </verify>
  <done>src/retrieval.ts is successfully implemented with robust RAG helpers, safe metadata parsing, and claim grounding validation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [x] src/retrieval.ts exists and exports `extractChunks`.
- [x] src/retrieval.ts exports `sourceMetadata` compiling safe objects containing `{ id, source }`.
- [x] src/retrieval.ts exports `checkContextGrounding` validating reply keywords against context.
- [x] `npm run typecheck` passes with no errors.
- [x] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Retrieval parsing is fully isolated from API handlers.
- Safe `sources` payload formatting behaves predictably.
- Claim grounding validator successfully detects and flags unsupported Vitruvian keywords.
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-grounded-retrieval-and-safety-behavior/02-02-SUMMARY.md` summarizing the retrieval helpers, the grounding validation logic, and compilation verification.
</output>
