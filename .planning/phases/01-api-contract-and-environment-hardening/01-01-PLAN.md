---
phase: "01-api-contract-and-environment-hardening"
plan: "01"
name: "Normalize environment and local deployment contract"
wave: 1
depends_on: []
autonomous: false
agents:
  - engineering-infrastructure-devops
  - engineering-security-engineer
requirements:
  - R1
  - R2
  - R9
files_modified:
  - "wrangler.jsonc"
  - ".dev.vars.example"
  - "README.md"
files_forbidden:
  - "src/index.ts"
  - "package.json"
  - "worker-configuration.d.ts"
  - "tests/"
  - "scripts/"
  - "kb/"
  - "dataset/"
  - "train_phoenix.py"
sequential_files: []
expected_artifacts:
  - path: "wrangler.jsonc"
    provides: "Cloudflare Worker config with canonical non-secret vars only"
    required: true
  - path: ".dev.vars.example"
    provides: "Local development secret template without real credentials"
    required: true
  - path: "README.md"
    provides: "Setup, local dev, deployment, and secret-name contract documentation"
    required: true
must_haves:
  truths:
    - "The canonical Workers AI token secret is `CF_API_TOKEN`."
    - "`AZURE_OPENAI_API_KEY` is not present in `wrangler.jsonc` vars."
    - "`MODEL_PROVIDER` documents exactly `workersai` and `azureopenai`."
    - "Debug enablement uses non-secret env plus a separate secret token."
  artifacts:
    - path: ".dev.vars.example"
      min_lines: 8
      contains:
        - "CF_API_TOKEN="
        - "PHOENIX_DEBUG_TOKEN="
        - "AZURE_OPENAI_API_KEY="
    - path: "README.md"
      min_lines: 40
      contains:
        - "wrangler secret put CF_API_TOKEN"
        - "MODEL_PROVIDER"
        - "npm run typecheck"
  key_links:
    - "`wrangler.jsonc` var names must match the README environment table."
    - "Secret names in `.dev.vars.example` must match README secret setup commands."
verification_commands:
  - node -e "const fs=require('fs'); const c=fs.readFileSync('wrangler.jsonc','utf8'); if(c.includes('AZURE_OPENAI_API_KEY')) process.exit(1); if(!c.includes('\"MODEL_PROVIDER\"')) process.exit(1); if(!c.includes('\"PHOENIX_DEBUG_ENABLED\"')) process.exit(1);"
  - node -e "const fs=require('fs'); const s=fs.readFileSync('.dev.vars.example','utf8'); for (const token of ['CF_API_TOKEN=','PHOENIX_DEBUG_TOKEN=','AZURE_OPENAI_API_KEY=']) if(!s.includes(token)) process.exit(1);"
  - node -e "const fs=require('fs'); const s=fs.readFileSync('README.md','utf8'); for (const token of ['wrangler secret put CF_API_TOKEN','MODEL_PROVIDER','npm run typecheck']) if(!s.includes(token)) process.exit(1);"
---

<objective>
Normalize the Phase 1 environment contract before Worker code changes depend on it. The result must make `wrangler.jsonc`, local `.dev.vars` expectations, and deployment docs agree on non-secret vars, secret names, provider values, and debug controls.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/phases/01-api-contract-and-environment-hardening/01-CONTEXT.md
@.planning/explorations/2026-05-27-phoenix-coach-cloudflare-mvp-design.md
@wrangler.jsonc
@README.md
@.gitignore
</context>

<execution_contract>
Role: infrastructure and security implementation.

Task: establish the canonical environment and secret contract for Phase 1 without touching runtime Worker code.

Scope:
- Read targets: `.planning/phases/01-api-contract-and-environment-hardening/01-CONTEXT.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/CODEBASE.md`, `.planning/explorations/2026-05-27-phoenix-coach-cloudflare-mvp-design.md`, `wrangler.jsonc`, `README.md`, `.gitignore`.
- Write targets: `wrangler.jsonc`, `.dev.vars.example`, `README.md`.
- Forbidden writes: `src/index.ts`, `package.json`, `worker-configuration.d.ts`, `tests/`, `scripts/`, `kb/`, `dataset/`, `train_phoenix.py`.

Allowed tools/actions:
- Use read-before-write, evidence-before-action, minimal diff, verify-before-report.
- Edit only the declared write targets.
- Add `.dev.vars.example` with placeholder values only.
- Run the verification commands listed in frontmatter.

Forbidden actions:
- Do not put real secrets, tokens, account IDs, or deployment credentials in committed files.
- Do not add provider aliases beyond `workersai` and `azureopenai`.
- Do not change `src/index.ts`; Plan 01-02 owns the runtime code.
- Do not add dependencies.
- Do not use destructive git operations.

Implementation sequence:
1. Read all declared sources and confirm current `wrangler.jsonc` vars.
2. Update `wrangler.jsonc` so `vars` contains only non-secret values: `CF_ACCOUNT_ID`, `AI_GATEWAY_ID`, `MODEL_PROVIDER`, `WORKERS_AI_MODEL`, `AZURE_RESOURCE_NAME`, `AZURE_DEPLOYMENT_NAME`, `AZURE_API_VERSION`, `ALLOWED_ORIGIN`, and `PHOENIX_DEBUG_ENABLED`.
3. Remove `AZURE_OPENAI_API_KEY` from `wrangler.jsonc` vars. Do not add `CF_API_TOKEN` or `PHOENIX_DEBUG_TOKEN` to vars.
4. Create `.dev.vars.example` with placeholder entries for `CF_API_TOKEN`, `PHOENIX_DEBUG_TOKEN`, and optional `AZURE_OPENAI_API_KEY`, plus comments explaining that real `.dev.vars` remains ignored.
5. Replace the placeholder README with concise setup docs covering install, typecheck, local dev, secret commands, provider values, debug token behavior, and deployment notes.
6. Run verification.

Required interfaces/content structure:
- `MODEL_PROVIDER` values are exactly `workersai` and `azureopenai`.
- `PHOENIX_DEBUG_ENABLED` is a string-like non-secret flag with default `"false"`.
- README must include `wrangler secret put CF_API_TOKEN`, optional `wrangler secret put AZURE_OPENAI_API_KEY`, `npm run typecheck`, `npm run dev`, and `npm run deploy`.

Edge/error cases:
- If current docs mention `CLOUDFLARE_API_TOKEN`, replace it with `CF_API_TOKEN` unless explaining historical context is necessary.
- If `.dev.vars.example` would be ignored by `.gitignore`, update only the ignore rule required to allow the example file, and add `.gitignore` to `files_modified` plus this plan's artifact lists before continuing. Otherwise emit `BLOCKED`.
- If a real secret is already present in any planned write target, emit `BLOCKED` and report the file path without printing the secret value.

Verification criteria:
- Frontmatter `verification_commands` pass.
- `git diff --check` reports no whitespace errors.

Final result format:
- Status: completed or BLOCKED.
- Files changed.
- Verification commands and results.
- Any decisions or issues.
</execution_contract>

<tasks>
<task id="01" name="Normalize wrangler non-secret vars">
<action>
Read `wrangler.jsonc`, the Phase 1 context, and the exploration design. Keep the existing `ai_search` binding exactly as `KB` / `phoenix-vitruvian-kb` / `remote: true`. In `vars`, keep `MODEL_PROVIDER` defaulted to `"workersai"`, keep `WORKERS_AI_MODEL`, keep Azure resource/deployment/version placeholders as non-secret route metadata, remove `AZURE_OPENAI_API_KEY`, and add `PHOENIX_DEBUG_ENABLED` defaulted to `"false"`. Do not add any secret-bearing key to `vars`.

> verification: node -e "const fs=require('fs'); const c=fs.readFileSync('wrangler.jsonc','utf8'); if(c.includes('AZURE_OPENAI_API_KEY')) process.exit(1); if(!c.includes('\"MODEL_PROVIDER\"')) process.exit(1); if(!c.includes('\"PHOENIX_DEBUG_ENABLED\"')) process.exit(1);"
</action>
<verify>
node -e "const fs=require('fs'); const c=fs.readFileSync('wrangler.jsonc','utf8'); if(c.includes('AZURE_OPENAI_API_KEY')) process.exit(1); if(!c.includes('\"MODEL_PROVIDER\"')) process.exit(1); if(!c.includes('\"PHOENIX_DEBUG_ENABLED\"')) process.exit(1);"
</verify>
<done>Done when `wrangler.jsonc` has canonical non-secret vars and no secret placeholders.</done>
</task>

<task id="02" name="Add local secret template">
<action>
Create `.dev.vars.example` with placeholder values only. It must include `CF_API_TOKEN=`, `PHOENIX_DEBUG_TOKEN=`, and optional `AZURE_OPENAI_API_KEY=`. Include comments telling developers to copy it to `.dev.vars` locally and never commit real values. Keep placeholder values empty or obvious placeholders; do not invent fake token-shaped values.

> verification: node -e "const fs=require('fs'); const s=fs.readFileSync('.dev.vars.example','utf8'); for (const token of ['CF_API_TOKEN=','PHOENIX_DEBUG_TOKEN=','AZURE_OPENAI_API_KEY=']) if(!s.includes(token)) process.exit(1);"
</action>
<verify>
node -e "const fs=require('fs'); const s=fs.readFileSync('.dev.vars.example','utf8'); for (const token of ['CF_API_TOKEN=','PHOENIX_DEBUG_TOKEN=','AZURE_OPENAI_API_KEY=']) if(!s.includes(token)) process.exit(1);"
</verify>
<done>Done when the local secret template exists and contains only non-sensitive placeholders.</done>
</task>

<task id="03" name="Document setup, provider, and deployment contract">
<action>
Rewrite `README.md` from the current title-only stub into a concise operational doc. Include sections for purpose, install, local development, required vars/secrets, provider values, debug access, typecheck, local smoke testing placeholder, and deploy. Document `CF_API_TOKEN` as the canonical Workers AI secret, `AZURE_OPENAI_API_KEY` as optional only when `MODEL_PROVIDER=azureopenai`, and `PHOENIX_DEBUG_TOKEN` as required only for trusted debug. Do not document real token values.

> verification: node -e "const fs=require('fs'); const s=fs.readFileSync('README.md','utf8'); for (const token of ['wrangler secret put CF_API_TOKEN','MODEL_PROVIDER','npm run typecheck']) if(!s.includes(token)) process.exit(1);"
</action>
<verify>
node -e "const fs=require('fs'); const s=fs.readFileSync('README.md','utf8'); for (const token of ['wrangler secret put CF_API_TOKEN','MODEL_PROVIDER','npm run typecheck']) if(!s.includes(token)) process.exit(1);"
</verify>
<done>Done when README states the same environment contract as `wrangler.jsonc` and `.dev.vars.example`.</done>
</task>
</tasks>

<stop_gates>
- Emit `BLOCKED` if any planned write target contains a real credential or token value.
- Emit `BLOCKED` if `.dev.vars.example` cannot be committed because of ignore rules and `.gitignore` is not added to the plan scope first.
- Emit `BLOCKED` if the executor cannot determine whether a key is secret or non-secret from the Phase 1 context and current source.
- Emit `BLOCKED` if verification cannot be run.
</stop_gates>

<recovery>
After interruption or compaction, re-read this plan, `.planning/phases/01-api-contract-and-environment-hardening/01-CONTEXT.md`, and any partial result artifact. Inspect `git diff` and compare changed files against `files_modified` and `files_forbidden`. Continue only from tasks whose verification has not passed. Re-run all frontmatter verification commands and `git diff --check` before reporting.
</recovery>

<verification>
- [ ] `wrangler.jsonc` has canonical non-secret vars only.
- [ ] `.dev.vars.example` exists and contains placeholder secret names with no values.
- [ ] README documents install, local dev, secrets, provider values, typecheck, and deploy.
- [ ] `git diff --check` passes.
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- R1 environment and secret naming contract is documented before runtime changes.
- R2 retains the existing AI Search binding shape for later smoke verification.
- R9 debug control names are documented without exposing secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-contract-and-environment-hardening/01-01-SUMMARY.md` with changed files, verification output, and any blocked evidence.
</output>
